---
import Layout from '../layouts/Layout.astro';
import '../styles/index.css';

const linkedInUrl = "https://www.linkedin.com/in/selim-boubtane/";
---

<Layout title="Contact - Selim BOUBTANE">
    <section style="max-width: 800px; margin: 0 auto; padding: 2rem; text-align: justify; line-height: 1.6;">
        
        <h1 class="fade-in" style="font-size: 2.5rem; font-weight: bold; margin-bottom: 1.5rem; text-align: center;">Contactez-moi</h1>
        
        <p class="fade-in" style="margin-bottom: 1.5rem; font-size: 1.1rem;">
            Vous souhaitez collaborer ou discuter de vos projets ? Je suis disponible pour échanger et découvrir vos idées. Contactez-moi directement sur LinkedIn ou utilisez le formulaire ci-dessous.
        </p>
        
        <!-- Bouton LinkedIn -->
        <div class="fade-in" style="text-align: center; margin-top: 2rem;">
            <a href={linkedInUrl} target="_blank" rel="noopener noreferrer" style="display: inline-block; padding: 1rem 2rem; font-size: 1.2rem; font-weight: bold; color: white; background-color: #0a66c2; border-radius: 8px; text-decoration: none; transition: background-color 0.3s ease, transform 0.3s ease;">
                Visitez mon LinkedIn
            </a>
        </div>

        <!-- Formulaire de contact -->
        <div class="fade-in form-container" style="margin-top: 3rem; padding: 2rem; border-radius: 20px;">
            <h2 style="font-size: 2rem; font-weight: 600; margin-bottom: 1.5rem; text-align: center;">Envoyez-moi un message</h2>
            <form action="https://formspree.io/f/xkgnzngn" method="POST" style="display: flex; flex-direction: column; gap: 1rem; max-width: 600px; margin: 0 auto;">
                <label for="name" style="font-weight: bold;">Nom</label>
                <input type="text" id="name" name="name" placeholder="Votre nom" required class="form-input">
        
                <label for="email" style="font-weight: bold;">Email</label>
                <input type="email" id="email" name="_replyto" placeholder="Votre adresse email" required class="form-input">
        
                <label for="message" style="font-weight: bold;">Message</label>
                <textarea id="message" name="message" rows="5" placeholder="Votre message" required class="form-input"></textarea>
        
                <button type="submit" class="form-button">
                    Envoyer
                </button>
            </form>
        </div>
        
    </section>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // Gestion du défilement pour l'animation fade-in
            const faders = document.querySelectorAll('.fade-in');
            const appearOptions = { threshold: 0.3, rootMargin: '0px 0px -50px 0px' };
            const appearOnScroll = new IntersectionObserver(function (entries, observer) {
                entries.forEach(entry => {
                    if (!entry.isIntersecting) return;
                    entry.target.classList.add('show');
                    observer.unobserve(entry.target);
                });
            }, appearOptions);
            faders.forEach(fader => appearOnScroll.observe(fader));
        });
    </script>
</Layout>
